# 작업일지 — 2026-06-02

## 완료된 작업

### Vercel 배포 (W6)
- GitHub 연동 → `student-ai-os.vercel.app` 프로덕션 배포 완료
- 환경변수 3개 등록: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `GROQ_API_KEY`
- main 브랜치 푸시 시 자동 배포 파이프라인 구성

### 모바일 반응형 대응
- `Sidebar.tsx` — `hidden md:flex` 추가, 모바일에서 사이드바 숨김
- `BottomNav.tsx` 신규 — 모바일 하단 고정 네비게이션 (대시보드/과제/오늘/시험/과목)
- `layout.tsx` — `<BottomNav />` 추가, main 패딩 `p-4 pb-20 md:p-6`으로 모바일 최적화
- `StatsGrid.tsx` — `grid-cols-2` 모바일 2열, 폰트 `text-2xl sm:text-3xl`
- `TodayPreview.tsx` — 카드 그리드 모바일 1열

### 성능 개선 — Server Component 전환
- `/subjects/page.tsx` → Server Component, `SubjectsClient.tsx`로 인터랙션 분리
- `/exams/page.tsx` → Server Component, `ExamsClient.tsx`로 인터랙션 분리
- `useEffect` 초기 fetch 제거 → 페이지 진입 시 데이터 즉시 표시

---

## 세션 2 (오후) — 고정 일정 · Codex 피드백 · 완료 처리 · AI 수정

### 캘린더 페이지 + 고정 일정(schedules) 기능
- `/calendar/page.tsx` 신규 — assignments, exams, schedules 병렬 fetch
- `src/types/index.ts` — `Schedule`, `ScheduleCategory` 타입 추가
- `src/lib/actions/schedules.ts` 신규 — `getSchedules`, `createSchedule`, `deleteSchedule`
- `CalendarSection.tsx` 전면 재작성 — 날짜 클릭 패널에 과제/고정일정 탭, 고정 일정 CRUD
- `BottomNav.tsx` 개편 — 더보기 시트 패턴으로 5+개 메뉴 처리 (시험/과목/설정/로그아웃)
- `Header.tsx`, `Sidebar.tsx` — 캘린더 항목 추가
- `today/page.tsx` — `getSchedules()` 연결

### Codex 피드백 수정 (5건)
| 이슈 | 수정 |
|------|------|
| 생성 후 로컬 id 불일치 | `createSchedule`이 real DB row 반환, 임시 UUID 제거 |
| getSchedules user 필터 없음 | `user_id` 필터 명시 추가 |
| blocked hours 배너만 표시 | `recommend()`에 `availableHours` 실제 차감 적용 |
| UTC 날짜 버그 (읽기) | `format(date, "yyyy-MM-dd")`로 교체 |
| 겹치는 일정 중복 합산 | interval merge 알고리즘 적용 |

### 날짜 저장 UTC 버그 수정
- `createSchedule` date 타입 `Date → string`, 클라이언트에서 `format(date, "yyyy-MM-dd")` 전달

### 오늘 할 일 — 완료 처리 기능
- 추천 카드에 완료 버튼(체크 아이콘) 추가
- 완료 시 로컬 result 즉시 제거 (optimistic) + `router.refresh()`로 대시보드 반영
- `updateAssignmentStatus`에 `revalidatePath("/today")` 추가

### AI 학습 코치 일본어 혼용 수정
- system prompt에 한국어 전용 제약 명시 (まず 등 일본어 토큰 금지 조건 추가)
- `temperature` 0.7 → 0.3

---

## 다음 세션 시작 시 할 일

1. **직접 사용하면서 불편한 점 수집** — 실사용 피드백 기반 개선
2. **베타 사용자 공유** — 지인 3명에게 URL 공유
3. **subjects 캐싱** — `unstable_cache` 적용 (자주 안 바뀌는 데이터)

# 작업일지 — 2026-06-05

## 완료된 작업

### 3순위: 오늘 추천 품질 개선 (3가지 전부)

#### 1) 추천 이유 명확화 (`priority.ts`)
- `buildPrioritySummary()` 함수 추가 — 마감일/난이도/시험 충돌 조합별 1문장 설명 생성
- `ScoredAssignment`에 `prioritySummary: string` 필드 추가
- `RecommendCard`에 추천 이유 문구 표시 (제목 바로 아래 파란 텍스트)

#### 2) 시간 부족 시 대체 플랜 (`priority.ts`, `TodayPlanner.tsx`)
- `RecommendResult` 타입 변경: `ScoredAssignment[]` → `{ scheduled, overflow, overdue }`
- `OverflowPlan` 컴포넌트 추가 — 오늘 시간이 부족한 과제 + "내일 이어서" 안내
- 부분 배정 과제(e.g. 3시간짜리에 1시간만 배정)도 overflow에 포함, `remainingHours` 필드로 남은 시간 명시
- `remainingHours: number` 필드 `ScoredAssignment`에 추가 (타입 명확화)

#### 3) 완료 후 즉시 재계산 (`TodayPlanner.tsx`)
- `handleComplete`에서 완료 과제 제외 후 `recommend()` 즉시 재실행
- AI 코멘트도 동시에 초기화 (구 추천 기준 코멘트가 남는 문제 해결)

### 마감 초과 과제 별도 섹션 분리 (`priority.ts`, `TodayPlanner.tsx`)
- `daysLeft < 0` 과제를 scoring/배정 루프에서 완전히 분리 → `overdue[]` 배열로 반환
- 정렬 로직 변경: 버킷(0~4) 우선 → 같은 버킷 안에서 점수 내림차순
  - 버킷: 오늘(0) / 내일(1) / 2~3일(2) / 4~7일(3) / 8일+(4)
  - 마감 초과가 1순위를 밀어내던 문제 해결
- `OverduePlan` 컴포넌트 추가 — 주황색 테마, "N일 초과", 제출 가능 여부 확인 안내

### AI race condition 수정 (`TodayPlanner.tsx`)
- `aiRequestId = useRef(0)` 추가, 요청마다 채번
- 구 요청 응답 도착 시 ID 불일치 → 무시 처리
- 완료 처리 시 `aiRequestId.current++` + `setAiLoading(false)` 동시 호출

### 대시보드 날짜 라벨 수정 (`TodayPreview.tsx`)
- `daysLeft === 0 → "오늘 마감"`, `daysLeft === 1 → "내일 마감"` (priority.ts 기준 통일)
- `isUrgent` 기준 `<= 2` → `<= 1`로 좁힘

### AI 일본어 혼입 방지 (`ai.ts`)
- 시스템 프롬프트를 영어로 변경, 한국어 연결어 예시 명시
- `stripJapanese()` 후처리 함수 추가
  - 일본어 구두점(、。) → 공백 치환 후 히라가나·가타카나 제거 (문장 붙음 방지)
  - 제거 후 고립 구두점 정리 5단계 체이닝
- `daysLeft` dead code 제거, `dueLabel` ("오늘" / "내일" / "N일 후") 로 개선

### 코드리뷰 (`docs/코드리뷰/2026-06-05.md`)
- 4개 파일 전체 리뷰 작성 (P1~P3 총 9건)
- P1 → P3 재분류 포함, 수정 가능한 4건 즉시 반영

---

## 다음 세션 시작 시 할 일
- `calcBlockedHours` useMemo 적용 (TodayPlanner.tsx)
- 연속 완료 처리 stale prop 문제 개선 필요 여부 판단 (현재 허용 범위)
- Vercel 배포 후 실기기 QA

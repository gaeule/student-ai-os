# 작업일지 — 2026-06-04

## 완료된 작업

### Codex 코드리뷰 전체 반영
- **[P1] ExamsClient**: `useState` + `useEffect` 제거 → `initialExams`, `initialSubjects` 직접 렌더링 (react-hooks 린트 에러 수정)
- **[P1] proxy.ts**: PROTECTED_PATHS에 `/calendar`, `/exams` 추가 (비로그인 접근 차단)
- **[P2] parseDateOnly()** 도입 — exams/schedules/assignments `fromDb`에서 UTC 파싱 오차 제거
- **[P2] assignments.ts**: `dueDate` 타입 `Date→string`, 클라이언트에서 `format(date,"yyyy-MM-dd")` 전달
- **[P2] revalidatePath 전체 통일**: exams/assignments 모든 액션에 `/calendar`, `/today` 추가
- **[P3] AssignmentForm**: `watch→useWatch` 교체 (React Compiler 호환), fallback "알 수 없는 과목"
- **[P3] TodayPlanner**: 미사용 `isPending` 제거
- `npm run lint` 에러/경고 0개 달성

### 모바일 페이지 이동 속도 개선
- 주요 6개 페이지에 `loading.tsx` 추가 (assignments, today, calendar, exams, subjects, dashboard)
- 클릭 즉시 스켈레톤 UI 표시 → "멈춘 느낌" 제거

### 안정화 로드맵 저장
- 1순위 날짜/캐시 안정화 ✅ 완료
- 2순위 모바일 실사용 QA (진행 중)
- 3순위 오늘 추천 품질 개선

---

## 다음 세션 시작 시 할 일
- Vercel 배포 후 모바일에서 loading 스켈레톤 체감 확인
- QA 체크리스트 나머지 항목 진행

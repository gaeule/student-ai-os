# 작업일지 — 2026-06-01

## 완료된 작업

### W3 — 과제 입력 기능 완료
- **이미지 업로드 + AI 파싱** (`src/lib/actions/image-parse.ts`)
  - Supabase Storage(`assignment-image` 버킷) 업로드
  - Groq Vision API (`meta-llama/llama-4-scout-17b-16e-instruct`) 호출
  - 과제명/과목/마감일/난이도/소요시간 자동 추출 → 폼 자동 입력
- `next.config.ts` — Server Action bodySizeLimit 5mb로 설정

### subjects 테이블 + 과목 관리
- `subjects` 테이블 생성 (Supabase SQL)
- `assignments.subject text` → `subject_id uuid` 마이그레이션
- `src/lib/actions/subjects.ts` — CRUD Server Action 신규
- `src/app/(app)/subjects/page.tsx` — 과목 관리 페이지 신규
  - 과목 추가/수정/삭제 + 교수명/학기 입력

### 과제 수정 기능
- `updateAssignment` Server Action 추가
- `AssignmentForm.tsx` — edit 모드 지원 (defaultValues 주입)
- `AssignmentList.tsx` — 연필 아이콘 수정 버튼 추가
- `AssignmentsContainer.tsx` — edit 상태 관리 추가

### 타입 수정
- `Assignment` 타입: `subject: string` → `subjectId`, `subjectName`
- `Subject` 타입 신규 추가

### 버그 수정
- `ai.ts` — `a.subject` → `a.subjectName` (subject 컬럼 제거로 인한 런타임 에러)
- `TodayPlanner.tsx` — 동일 수정
- Groq Vision 모델명 수정: `llama-4-scout-17b-16e-instruct` → `meta-llama/llama-4-scout-17b-16e-instruct`
- Storage RLS 정책 버킷명 `assignment-images` → `assignment-image` 수정

---

## 트러블슈팅

| 문제 | 원인 | 해결 |
|---|---|---|
| 서버 켜면 맥북 메모리 부족 | next dev webpack 메모리 과다 사용 | `--turbopack` 플래그 추가 |
| Body exceeded 1MB | Server Action 기본 제한 | `bodySizeLimit: "5mb"` 설정 |
| 이미지 업로드 RLS 오류 | 버킷명 불일치 | 정책 재생성 |
| Groq Vision 404 | 모델명에 네임스페이스 누락 | `meta-llama/` prefix 추가 |
| AI 코멘트 에러 | subject → subjectName 미반영 | ai.ts, TodayPlanner 수정 |

---

## AI 현재 한계 (다음 개선 예정)
현재 AI 학습 코치가 아는 정보: 과제명, 과목, 난이도, 마감일, 오늘 배정 시간
모르는 정보: 우선순위 산출 근거, 시험 일정, 이전 학습 이력
→ W4에서 시험 일정 연동 시 조언 품질 크게 향상 예상

---

## 다음 세션 시작 시 할 일
1. `exams` 테이블 `subject text` → `subject_id uuid` 마이그레이션
2. 시험 등록 항목 확정 (과목, 유형, 날짜, 범위, 준비 시작일)
3. W4 AI 우선순위 엔진 구현
